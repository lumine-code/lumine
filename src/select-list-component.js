"use strict";

const InputDialogComponent = require("./input-dialog-component");

/** Private Etch renderer boundary for a SelectList model. @private */
module.exports = class SelectListComponent extends InputDialogComponent {};
